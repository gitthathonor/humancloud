<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="site.metacoding.humancloud.domain.board.BoardDao">
	<insert id="save">
		INSERT INTO board(board_title, board_content,
		board_read_count, board_user_id, board_created_at)
		VALUES(#{boardTitle}, #{boardContent}, 0,
		#{boardUserId}, now())
	</insert>
	<select id="findById"
		resultType="site.metacoding.humancloud.domain.board.Board">
		SELECT *
		FROM board
		WHERE board_id = #{boardId}
	</select>
	<select id="findAll"
		resultType="site.metacoding.humancloud.domain.board.Board">
		SELECT *
		FROM board
	</select>
	<update id="update">
		UPDATE board SET board_title = #{boardTitle}, board_content = now(), board_created_at = #{boardCreatedAt} WHERE board_id = #{boardId}
	</update>
	<delete id="deleteById">
		DELETE FROM board WHERE board_id = #{boardId}
	</delete>
</mapper>