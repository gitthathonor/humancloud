<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.humancloud.domain.job.JobDao">

	<select id="findAll" resultType="site.metacoding.humancloud.domain.job.Job">
		SELECT * FROM job
	</select>

	<insert id="save">
		INSERT INTO job(job_resume_id, job_recruit_id, job_name)
		VALUES(#{jobResumeId}, #{jobRecruitId}, #{JobName})
	</insert>

	<select id="findById" resultType="site.metacoding.humancloud.domain.job.Job">
		SELECT * FROM job WHERE job_id=#{resumeId}
	</select>

	<update id="update">
    UPDATE job SET job_name = #{JobName} WHERE job_id=#{resumeId}
  </update>

	<delete id="deleteById">
		DELETE FROM job WHERE job_id=#{resumeId}
	</delete>
</mapper>